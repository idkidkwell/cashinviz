# Optional: a free project ID from https://cloud.walletconnect.com
# Leave unset (or keep the placeholder) to DISABLE WalletConnect. The
# Connect Wallet modal will still work instantly with MetaMask / injected /
# Coinbase / Rainbow — no WSS relay probe on mount.
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Relayer URL (local dev)
NEXT_PUBLIC_RELAYER_URL=http://localhost:4000
